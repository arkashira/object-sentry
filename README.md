# Object Sentry
Object Sentry is a Python library that automatically detects and selects the optimal hardware accelerator for maximum performance.

## Installation
To install Object Sentry, run the following command:
